{
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-google": "^0.13.0",
    "jest": "^24.8.0"
  },
  "scripts": {
    "test": "jest",
    "eslint": "eslint src/ --fix",
    "quality": "yarn eslint && yarn test"
  }
}
